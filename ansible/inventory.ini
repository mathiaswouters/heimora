# Provisioning always runs against the local machine being set up.
# If you later want to push this to other hosts over SSH, add them
# here under a real group instead of [local].
[local]
localhost ansible_connection=local
